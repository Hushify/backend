namespace Hushify.Api.Constants;

public static class AppConstants
{
    public const string UserRateLimit = nameof(UserRateLimit);
    public const string IpRateLimit = nameof(IpRateLimit);
    public const string EmailCodeLimit = nameof(EmailCodeLimit);
}